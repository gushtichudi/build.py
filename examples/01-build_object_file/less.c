#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

void love_bomb(bool fruity) {
    // TODO: if doesn't compile, change this to USER
    char *user = getenv("USERNAME");

    if (!fruity)
        printf("%s, ily. k bye gotta go 2 miami\n", user);
        return;

    printf("OMG ASADJDAJGFMN %s I FUCKING LVE YOU SO CAMKCFSJKMC,AVD NBN NBN B\n", user);
    return;
}